---
name: policy-data-pipeline
description: 국내외 공공·국제기구 통계를 수집해 표준 스키마 패널로 정리하고, 검증·코드북·대시보드까지 자동 생성하는 정책연구용 데이터 파이프라인. KOSIS, 공공데이터포털, World Bank, OECD, Eurostat에서 지표를 가져오라는 요청, 기존 패널에 지표나 연도를 추가하라는 요청, 수집 데이터의 정합성을 검증하거나 코드북을 갱신하라는 요청, 정기 갱신을 실행하라는 요청에 사용한다.
---

# 정책연구 데이터 파이프라인

## 원칙

**값을 읽지 말고, 값을 읽는 코드를 쓴다.** 응답 본문에서 숫자를 직접 옮겨 적지 않는다. 스크립트가 파싱하게 하고 출력을 확인한다.

**검증에 실패하면 멈춘다.** 부분 성공을 정상 종료로 처리하지 않는다. 중단 조건은 `references/schema.md`에 있다.

**판정은 기록한다.** 규칙으로 처리하지 못해 판단이 들어간 건은 코드북 4절에 미확인 상태로 남긴다. 연구자가 확인한 뒤에만 사전에 등록한다. 임의로 확정하지 않는다.

## 절차

### 새 지표를 추가할 때

1. `references/api-registry.md`에서 해당 기관을 찾는다. 없으면 API 문서를 읽고 명세를 **먼저 등록**한다. 등록 전에 실제 호출 1건으로 검증하고, 문서와 응답이 다르면 응답을 따른다.
2. `config/sources.yml`에 소스 항목을 추가한다. 지표 코드, 단위, vintage, 지역 수준을 채운다.
3. `scripts/normalize.py`에 해당 소스의 변환 함수를 추가한다. 표준 스키마 10개 컬럼으로 맞춘다.
4. `config/sources.yml`의 `validation.value_range`에 지표별 허용 범위를 등록한다.
5. `python run.py --offline`으로 먼저 돌려보고, 통과하면 실시간으로 돌린다.

### 정기 갱신을 실행할 때

1. `python run.py`를 실행한다.
2. 검증에서 중단되면 원인을 보고하고 멈춘다. 넘어가지 않는다.
3. "확인 필요"가 나오면 해당 값의 원문을 대조한다. 원자료와 일치하면 코드북에 근거를 남긴다.
4. 코드북 4절에 미확인 항목이 있으면 사용자에게 알린다.

### 검증이 실패했을 때

원인을 먼저 말한다. 검사를 느슨하게 고쳐 통과시키지 않는다. 임계값을 바꿔야 한다면 왜 바꿔야 하는지 근거를 제시하고 사용자의 확인을 받는다.

## 표준 스키마

```
source | indicator_code | region_code | period | value | unit | vintage | retrieved_at | source_url | missing_reason
```

## 다른 기관으로 옮길 때

`references/api-registry.md`의 소스 목록과 `config/sources.yml`의 지표 정의만 바꾼다. `scripts/`는 손대지 않는다.
